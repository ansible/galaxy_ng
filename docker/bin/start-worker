#!/bin/bash

set -o errexit
set -o nounset

readonly RELOAD_ENABLED="${RELOAD_ENABLED:-no}"

if [[ "${RELOAD_ENABLED}" == 'yes' ]]; then
    exec pulpcore-worker --reload
else
    exec pulpcore-worker
fi
