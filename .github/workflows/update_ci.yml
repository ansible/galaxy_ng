# WARNING: DO NOT EDIT!
#
# This file was generated by plugin_template, and is managed by it. Please use
# './plugin-template --github galaxy_ng' to update this file.
#
# For more info visit https://github.com/pulp/plugin_template
---
name: CI Update
on:
  schedule:
    # * is a special character in YAML so you have to quote this string
    # runs at 2:30 UTC every Sunday
    - cron: '30 2 * * 0'
  
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false

    steps:
      - uses: actions/checkout@v2

      - name: Configure Git with ansible name and email
        run: |
          git config --global user.name 'ansible'
          git config --global user.email 'pulp-infra@redhat.com'

      - name: Run update
        run: |
          .github/workflows/scripts/update_ci.sh

      - name: Create Pull Request for CI files
        uses: peter-evans/create-pull-request@v3
        with:
          token: ${{ secrets.RELEASE_TOKEN }}
          committer: ansible <pulp-infra@redhat.com>
          author: ansible <pulp-infra@redhat.com>
          title: 'Update CI files'
          body: '[noissue]'
          commit-message: |
            Update CI files

            [noissue]
          delete-branch: true
